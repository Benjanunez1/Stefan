# Stefan — asistente de voz de Benja

## Configurar

1. Copiá `.env.local.example` como `.env.local` (en Vercel esto se hace en Settings → Environment Variables, no como archivo).
2. Completá:
   - `ELEVENLABS_API_KEY`
   - `ELEVENLABS_VOICE_ID`
   - `ANTHROPIC_API_KEY`

## Correr en desarrollo
